[package]
name = "fd-audit"
description = "FerrumDeck audit log: append-only event trail"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true

[dependencies]
fd-core = { path = "../fd-core", package = "ferrumdeck-core" }

# Serialization
serde = { workspace = true }
serde_json = { workspace = true }

# Time
chrono = { workspace = true }

# Error handling
thiserror = { workspace = true }

# Tracing
tracing = { workspace = true }

# Regex for sensitive data detection
regex = { workspace = true }

[dev-dependencies]
tokio-test = { workspace = true }
# Integration tests construct deterministic ids for the golden-fixture
# regression — see tests/audit_record_schema_drift.rs.
ulid = { workspace = true }
